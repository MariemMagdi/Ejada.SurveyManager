@page "/surveys"
@using Volo.Abp.Application.Dtos
@using Ejada.SurveyManager.Surveys.Dtos
@using Ejada.SurveyManager.Surveys
@using Ejada.SurveyManager.Localization
@inject NavigationManager NavigationManager
@inject ISurveyAppService SurveyService
@inject Volo.Abp.AspNetCore.Components.Web.AbpBlazorMessageLocalizerHelper<SurveyManagerResource> LH

@inherits AbpCrudPageBase<ISurveyAppService, SurveyDto, Guid, PagedAndSortedResultRequestDto, CreateSurveyDto, UpdateSurveyDto>

<Card Class="shadow-sm">
    <CardHeader Class="bg-white border-bottom">
        <Row Class="align-items-center justify-content-between g-3">
            <Column ColumnSize="ColumnSize.IsAuto">
                <div class="d-flex align-items-center">
                    <Icon Name="IconName.Poll" Class="text-primary me-2" Size="IconSize.Large" />
                    <div>
                        <h4 class="mb-0 fw-bold">@L["Surveys"]</h4>
                        <small class="text-muted">@L["Manage your surveys"]</small>
                    </div>
                </div>
            </Column>
            <Column ColumnSize="ColumnSize.IsAuto">
                <Button Color="Color.Primary"
                        Clicked="GoToCreatePage"
                        Class="px-4">
                    <Icon Name="IconName.Add" />
                     @LH.Localize("Actions")
                </Button>
            </Column>
        </Row>
    </CardHeader>

    <CardBody Class="p-0">
        <DataGrid TItem="SurveyDto"
                  Data="Entities"
                  ReadData="OnDataGridReadAsync"
                  TotalItems="TotalCount"
                  ShowPager="true"
                  PageSize="PageSize"
                  ShowFilterRow="true"
                  Striped="true"
                  Hoverable="true"
                  Responsive="true"
                  Class="border-0">
            <DataGridColumns>
                <DataGridColumn TItem="SurveyDto" Field="@nameof(SurveyDto.Name)" Caption="@L["Survey Name"]" Sortable="true">
                    <DisplayTemplate Context="context">
                        <div class="d-flex align-items-center">
                            <Icon Name="IconName.File" Class="text-primary me-2" />
                            <strong>@context.Name</strong>
                        </div>
                    </DisplayTemplate>
                </DataGridColumn>
                <DataGridColumn TItem="SurveyDto" Field="@nameof(SurveyDto.Purpose)" Caption="@L["Purpose"]" Sortable="true">
                    <DisplayTemplate Context="context">
                        <span class="text-muted">@(context.Purpose?.Length > 50 ? context.Purpose.Substring(0, 50) + "..." : context.Purpose)</span>
                    </DisplayTemplate>
                </DataGridColumn>
                <DataGridColumn TItem="SurveyDto" Field="@nameof(SurveyDto.TargetAudience)" Caption="@L["Target Audience"]" Sortable="true">
                    <DisplayTemplate Context="context">
                        <Badge Color="Color.Info">@context.TargetAudience</Badge>
                    </DisplayTemplate>
                </DataGridColumn>
                <DataGridColumn TItem="SurveyDto" Field="@nameof(SurveyDto.IsActive)" Caption="@L["Is Active"]" Sortable="true">
                    <DisplayTemplate Context="context">
                        @if (context.IsActive)
                        {
                            <Badge Color="Color.Success">
                                <Icon Name="IconName.Check" />
                                @L["Active"]
                            </Badge>
                        }
                        else
                        {
                            <Badge Color="Color.Secondary">
                                <Icon Name="IconName.Times" />
                                @L["Inactive"]
                            </Badge>
                        }
                    </DisplayTemplate>
                </DataGridColumn>

                <DataGridEntityActionsColumn TItem="SurveyDto" @ref="@EntityActionsColumn" Caption="@L["Actions"]" Width="180px">
                    <DisplayTemplate Context="context">
                        <EntityActions TItem="SurveyDto" EntityActionsColumn="@EntityActionsColumn">
                            <EntityAction TItem="SurveyDto"
                                          Text="@LH.Localize("ViewDetails")"
                                          Icon="IconName.Eye"
                                          Clicked="() => GoToDetailsPage(context.Id)" />
                            <EntityAction TItem="SurveyDto"
                                          Text="@L["Edit"]"
                                          Icon="IconName.Edit"
                                          Clicked="() => GoToEditPage(context.Id)" />
                            <EntityAction TItem="SurveyDto"
                                          Text="@L["Delete"]"
                                          Icon="IconName.Trash"
                                          Color="Color.Danger"
                                          Clicked="() => DeleteEntityAsync(context)"
                                          ConfirmationMessage="() => GetDeleteConfirmationMessage(context)" />
                        </EntityActions>
                    </DisplayTemplate>
                </DataGridEntityActionsColumn>
            </DataGridColumns>
        </DataGrid>
    </CardBody>
</Card>

@code {
    private void GoToCreatePage()
    {
        NavigationManager.NavigateTo("/surveys/create");
    }

    private void GoToEditPage(Guid id)
    {
        NavigationManager.NavigateTo($"/surveys/{id}/edit");
    }

    private void GoToDetailsPage(Guid id)
    {
        NavigationManager.NavigateTo($"/surveys/{id}/details");
    }

    protected override string GetDeleteConfirmationMessage(SurveyDto entity)
    {
        return string.Format(L["Are you sure you want to delete the survey '{0}'?"], entity.Name);
    }
}
