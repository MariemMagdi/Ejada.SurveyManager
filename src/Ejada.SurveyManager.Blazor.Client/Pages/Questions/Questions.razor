@page "/questions"
@using Volo.Abp.Application.Dtos
@using Ejada.SurveyManager.Surveys
@using Ejada.SurveyManager.Surveys.Dtos
@using Ejada.SurveyManager.Localization
@inject NavigationManager NavigationManager
@inject IQuestionAppService QuestionService
@inject Volo.Abp.AspNetCore.Components.Web.AbpBlazorMessageLocalizerHelper<SurveyManagerResource> LH

@inherits AbpCrudPageBase<IQuestionAppService, QuestionDto, Guid, PagedAndSortedResultRequestDto, CreateQuestionDto, UpdateQuestionDto>

<div class="modern-page-wrapper">
    <div class="glass-card">
        <div class="gradient-header">
            <div class="gradient-header-content">
                <Row Class="align-items-center justify-content-between g-3">
                    <Column ColumnSize="ColumnSize.IsAuto">
                        <div class="d-flex align-items-center">
                            <div class="header-icon-box me-3">
                                <Icon Name="IconName.QuestionCircle" Size="IconSize.ExtraLarge" />
                            </div>
                            <div>
                                <h2 class="mb-1 fw-bold text-white">@LH.Localize("Questions")</h2>
                                <p class="text-white-50 mb-0">@LH.Localize("ManageYourQuestions")</p>
                            </div>
                        </div>
                    </Column>
                    <Column ColumnSize="ColumnSize.IsAuto">
                        <Button Color="Color.Primary"
                                Clicked="GoToCreatePage"
                                Class="px-4 btn-modern">
                            <Icon Name="IconName.Add" />
                            @LH.Localize("NewQuestion")
                        </Button>
                    </Column>
                </Row>
            </div>
        </div>

        <div class="px-4 pb-4">
            <DataGrid TItem="QuestionDto"
                  Data="Entities"
                  ReadData="OnDataGridReadAsync"
                  TotalItems="TotalCount"
                  ShowPager="true"
                  PageSize="PageSize"
                  ShowFilterRow="true"
                  Striped="true"
                  Hoverable="true"
                  Responsive="true"
                  Class="border-0 modern-table">
            <DataGridColumns>
                <DataGridColumn TItem="QuestionDto" Field="@nameof(QuestionDto.Text)" Caption="@LH.Localize("QuestionText")" Sortable="true">
                    <DisplayTemplate Context="context">
                        <div class="d-flex align-items-center">
                            <Icon Name="IconName.File" Class="text-info me-2" />
                            <span>@(context.Text?.Length > 60 ? context.Text.Substring(0, 60) + "..." : context.Text)</span>
                        </div>
                    </DisplayTemplate>
                </DataGridColumn>
                <DataGridColumn TItem="QuestionDto" Caption="@LH.Localize("QuestionType")" Sortable="true">
                    <DisplayTemplate Context="context">
                        <Badge Color="Color.Primary">@context.Type.ToString()</Badge>
                    </DisplayTemplate>
                </DataGridColumn>
                <DataGridColumn TItem="QuestionDto" Caption="@LH.Localize("Options")" Sortable="true">
                    <DisplayTemplate Context="context">
                        <Badge Color="Color.Info">
                            <Icon Name="IconName.List" />
                            @(context.Options?.Count ?? 0) @LH.Localize("Options")
                        </Badge>
                    </DisplayTemplate>
                </DataGridColumn>
                <DataGridColumn TItem="QuestionDto" Caption="@LH.Localize("Status")" Sortable="true">
                    <DisplayTemplate Context="context">
                        @if (context.IsLinkedToSurvey)
                        {
                            <Badge Color="Color.Warning">
                                <Icon Name="IconName.Link" />
                                @LH.Localize("LinkedToSurvey")
                            </Badge>
                        }
                        else
                        {
                            <Badge Color="Color.Success">
                                <Icon Name="IconName.Edit" />
                                @LH.Localize("Editable")
                            </Badge>
                        }
                    </DisplayTemplate>
                </DataGridColumn>

                <DataGridEntityActionsColumn TItem="QuestionDto" @ref="@EntityActionsColumn" Caption="@LH.Localize("Actions")" Width="180px">
                    <DisplayTemplate Context="context">
                        <EntityActions TItem="QuestionDto" EntityActionsColumn="@EntityActionsColumn">
                            <EntityAction TItem="QuestionDto"
                                          Text="@LH.Localize("ViewDetails")"
                                          Icon="IconName.Eye"
                                          Clicked="() => GoToDetailsPage(context.Id)" />
                            <EntityAction TItem="QuestionDto"
                                          Text="@LH.Localize("Edit")"
                                          Icon="IconName.Edit"
                                          Visible="@(!context.IsLinkedToSurvey)"
                                          Clicked="() => GoToEditPage(context.Id)" />
                            <EntityAction TItem="QuestionDto"
                                          Text="@LH.Localize("Delete")"
                                          Icon="IconName.Trash"
                                          Color="Color.Danger"
                                          Visible="@(!context.IsLinkedToSurvey)"
                                          Clicked="() => DeleteEntityAsync(context)"
                                          ConfirmationMessage="() => GetDeleteConfirmationMessage(context)" />
                        </EntityActions>
                    </DisplayTemplate>
                </DataGridEntityActionsColumn>
            </DataGridColumns>
        </DataGrid>
        </div>
    </div>
</div>

@code {
    private void GoToCreatePage()
    {
        NavigationManager.NavigateTo("/questions/create");
    }

    private void GoToEditPage(Guid id)
    {
        NavigationManager.NavigateTo($"/questions/{id}/edit");
    }

    private void GoToDetailsPage(Guid id)
    {
        NavigationManager.NavigateTo($"/questions/{id}/details");
    }

    protected override string GetDeleteConfirmationMessage(QuestionDto entity)
    {
        return string.Format(LH.Localize("Error.ConfirmDeleteQuestion"), entity.Text);
    }
}